# 設計方針

本教材は「型が通るが、実行時・連携時・運用時に壊れる」不具合を対象にします。Frontend、BFF、Backendの役割を分離することで、画面上の症状だけでは根本原因を断定できない構造にします。BFFはUI向けの集約と下流エラーの透過、Backendは業務処理・DB・キャッシュ・外部連携、WireMockは外部障害と契約差異の再現を担います。

Node側はNode標準テストランナーとTypeScriptコンパイラを採用します。型検査、挙動テスト、Git履歴を独立した証拠として扱うためです。Spring Boot側はJUnitでキャッシュ、XML、楽観ロック、トランザクション境界の重要契約を固定します。サンプルコード・章名・説明文は特定教材を複製していません。
