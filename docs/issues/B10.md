# B10: 同じ操作のログを追跡できない

## 現象
ブラウザから開始した一つの検索操作について、BFF、Backend、外部連携のログを一意に結びつけられません。

## 再現手順
1. 検索を一度実行します。
2. ブラウザの`X-Request-Id`を記録します。
3. 各サービスと外部連携で同じ値を検索します。

## 期待値
一つの操作がすべての層で同一の相関IDにより追跡できることです。
