# B08 解答: Transaction境界

## 現象と原因
業務処理を構成する更新が同一トランザクションに入らないと、途中例外でも先行更新だけが確定します。

## 調査順序
例外時のログ、DBの注文・在庫両方、呼出し階層、トランザクション属性を順に確認します。DBの不整合を先に観測することで、画面やBFFだけの問題を除外できます。

## 修正・回帰・再発防止
業務単位のサービスメソッドを`@Transactional`にし、失敗時に両テーブルが変わらない統合テストを追加します。非同期化や別サービス化の際は補償処理を設計します。
