# B06 解答: Lost Update

## 現象と原因
同じ在庫行を同時に読み、更新すると、後から保存した値が前の更新を上書きします。

## 調査順序
並行実行のrequestId、SQL更新、最終在庫数、Entityのversionを順に確認します。単一リクエストでは再現しないこと自体が競合の手掛かりです。

## 修正・回帰・再発防止
`@Version`による楽観ロックを使い、競合時は再試行または明示的エラーを返します。`ArchitectureContractsTest`がversionの存在を固定し、並行統合テストを追加します。
