# B02 解答: 空結果のキャッシュ汚染

## 現象と原因
一時的な外部障害により返った空配列をRedisへ保存すると、障害復旧後もTTL中は空配列が返ります。

## 調査順序
同一条件でWireMockの応答、Redis値・TTL、Backendのキャッシュヒットをこの順で比較します。外部が正常なのにRedisが空なら、保存条件が焦点になります。

## 修正・回帰・再発防止
空配列をキャッシュしないポリシーへ変更し、`CustomerCachePolicyTest`で固定します。空結果をキャッシュする要件が必要なら、障害種別・鮮度・短いTTLを明示します。
