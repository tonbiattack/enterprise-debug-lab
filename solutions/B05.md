# B05 解答: XML namespace契約

## 現象と原因
HTTP 200は通信成功を示すだけです。XML namespaceや要素名が期待する契約と異なれば、値へ変換できません。

## 調査順序
生のXML本文を保存し、Content-Type、namespace URI、要素名、変換例外を順に確認します。DTOやパーサーを読む前に契約差異を可視化します。

## 修正・回帰・再発防止
namespaceを考慮したパーサーで必須要素を検証し、正しいXMLと誤ったXMLの両方をテストします。外部契約のサンプル本文をバージョン管理します。
