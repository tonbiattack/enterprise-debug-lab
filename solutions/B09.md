# B09 解答: OpenAPI契約違反

## 現象と原因
型生成はOpenAPIの必須性を信頼します。実装がnullableでない項目にnullや空値を返すと、実行時にだけ破綻します。

## 調査順序
Frontendの生成型、BFFの実応答、Backendの実応答、OpenAPI定義を同じフィールドで比較します。どの境界で契約が破れたかを特定できます。

## 修正・回帰・再発防止
Backendで必須値を保証し、BFFの契約テストとFrontendの境界ガードを追加します。仕様変更時はOpenAPIと生成クライアントを同一PRで更新します。
