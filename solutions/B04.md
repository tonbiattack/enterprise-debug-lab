# B04 解答: WireMock本文ファイル

## 現象と原因
`bodyFileName`は`__files`配下の実ファイルを参照します。mappingが有効でも対象本文がないと、該当リクエストだけ失敗します。

## 調査順序
WireMockログ、選択されたmapping、`bodyFileName`、コンテナ内の`__files`を順に照合します。URLやアプリの変換処理を先に疑う必要はありません。

## 修正・回帰・再発防止
参照先の本文を追加し、コンテナ起動時にmapping参照の存在を検査します。スタブを単一のレビュー単位で管理します。
