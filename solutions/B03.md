# B03 解答: WireMockスタブ選択

## 現象と原因
同一URLのmappingは優先度と条件の両方で選択されます。条件が広すぎる、または優先度が逆転すると意図しない本文が返ります。

## 調査順序
実際のHTTPリクエストを保存し、mappingの`request`条件、`priority`、選択結果を順に照合します。アプリコードより先にスタブ選択を確定できます。

## 修正・回帰・再発防止
条件を相互排他的にし、標準応答を明確な優先度にします。代表条件ごとの統合テストと、スタブ変更時のレビューを維持します。
