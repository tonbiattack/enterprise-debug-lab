# B07 解答: N+1クエリ

## 現象と原因
一覧の親行ごとに関連データを遅延ロードすると、件数に比例してSQLが増えます。

## 調査順序
応答時間、SQLログの件数、親・子のアクセス順、リポジトリ定義を順に確認します。CPUではなくSQL回数が増えることを先に示します。

## 修正・回帰・再発防止
fetch join、EntityGraph、または集約専用クエリを選びます。代表件数でクエリ数・応答時間を測る統合テストを追加します。
